# Copy this file to .env and fill in your real keys:
#   cp .env.example .env
#
# .env is gitignored — real keys won't be committed.
# .env.example IS committed — it must contain only placeholders.

OPENAI_API_KEY=sk-proj-replace-with-your-real-key
# ANTHROPIC_API_KEY=sk-ant-replace-with-your-real-key